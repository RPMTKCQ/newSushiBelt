[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
eclipse.project.name = appName + '-core'
project(":core") {
  apply plugin: "java"


  dependencies {
    api "com.badlogicgames.ashley:ashley:$ashleyVersion"
    api "com.badlogicgames.gdx:gdx-ai:$aiVersion"
    api "com.badlogicgames.gdx:gdx-box2d:$gdxVersion"
    api "com.badlogicgames.gdx:gdx-freetype:$gdxVersion"
    api "com.badlogicgames.gdx:gdx:$gdxVersion"
    // freetype font support for skins
//  api "com.github.raelus.stripe:freetype:$stripeVersion"
    // ten patch for better ProgressBar support (scene2d)
//  api "com.github.raelus.TenPatch:tenpatch:$tenPatchVersion"
    api 'com.github.raeleus.stripe:freetype:2.0.0'
    api 'com.github.raeleus.TenPatch:tenpatch:5.2.3'

    if (enableGraalNative == 'true') {
      implementation "io.github.berstanio:gdx-svmhelper-annotations:$graalHelperVersion"
    }

  }
}
repositories {
  mavenCentral()
  maven { url 'https://jitpack.io' }
}

